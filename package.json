{
  "name": "github-repo-manager",
  "version": "1.0.0",
  "description": "No-friction GitHub push tool",
  "main": "dist/main/main.js",
  "scripts": {
"build": "tsc && mkdir -p dist/renderer && cp src/renderer/index.html src/renderer/style.css dist/renderer/",
    "start": "npm run build && electron .",
    "dev": "tsc && electron ."
  },
  "dependencies": {
    "@octokit/rest": "^20.0.0",
    "electron-store": "^8.1.0",
    "simple-git": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "electron": "^28.0.0",
    "typescript": "^5.3.3"
  }
}
